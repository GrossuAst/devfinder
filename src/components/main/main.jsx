import styles from './main.module.scss';
import Form from '../form/form';

const Main = () => {
    return (
        <main className={ styles.main }>
            <Form />
        </main>
    )
};

export default Main;